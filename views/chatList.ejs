<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="/main.css" rel="stylesheet">
    <title>Document</title>
</head>
<body class="grey-bg">

    <!-- include(파일위치)를 통해, js를 붙일 수 있음 -->
    <%- include('nav.ejs') %>

    <h4>나의 채팅방 목록 </h4>
    <div class="white-bg">
        <% for (let i = 0; i < 글목록.length; i++){ %>
            <div class="list-box">
                <h4>
                <a href="/chat/detail/<%= 채팅방목록[i]._id %>">
                    채팅방 <%= i %>
                </a>
            </h4>
            <p><%= 채팅방목록[i]._id %></p>
            </div>
        <% } %>
    </div> 

</body>
<script>
    window.onload = () => { 
        if(msg == '이미_만들어진_채팅방') {
            alert(`이미 해당 채팅방을 개설했습니다`);
        }else if(msg == '본인과_채팅은_불가능함'){
            alert(`본인과 채팅할 수 없음`);
        }
    }
</script>
</html>